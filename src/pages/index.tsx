import type { NextPage } from 'next';

import { Layout } from '@/components/layouts';

const Home: NextPage = () => {
  return (
    <Layout>
      <div></div>
    </Layout>
  );
};

export default Home;
